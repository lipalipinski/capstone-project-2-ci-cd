pipeline {
    agent {label 'ec2'}

    stages {
        stage('tf init') {
            steps {
                sh "terraform -chdir=tf-provision init"
            }
        }
        stage('tf apply') {
            steps {
                sh "terraform -chdir=tf-provision -auto-approve apply"
            }
        }
    }
}