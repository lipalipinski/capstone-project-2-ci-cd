pipeline {
    agent {label 'ec2'}

    stages {
        stage('tf init') {
            steps {
                terraform -chdir=tf-provision init
            }
        }
        stage('tf apply') {
            steps {
                terraform -chdir=tf-provision -auto-approve apply
            }
        }
    }
}